import 'package:flutter/material.dart';

class ChatsController extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    body : SafeArea(
      child : Column(
        children : [
          
        ]
      )
    )
  )
}